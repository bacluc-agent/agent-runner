Choose ONE issue for a single automated agent run.
Each candidate line is "number: title [labels] [created] [PR: none|open|merged|closed #<n> updated:<ts>] [last-human-feedback:<ts|none>] | body-excerpt"; use title+body+labels+PR+feedback to assess.
CANDIDATE ORDER IS RANDOM and carries no priority: NEVER prefer earlier lines or treat position as ranking.
Diversity is mandatory: do NOT pick any issue listed under Recently selected — avoid unless all other candidates are infeasible; rotate area/target-repo vs last 2 picks, meta tasks at most 1 in 4.
Breadth-first priority: (a) SKIP/DEPRIORITIZE where PR is open and last-human-feedback is none or older than PR updatedAt (awaiting human feedback) unless all else infeasible.
(b) PRIORITIZE untried PR:none to guarantee breadth; (c) PRIORITIZE where last-human-feedback is newer than PR updatedAt (human review/feedback ready, close-to-merge); (d) merged/closed PR is evidence of a prior attempt, not a blocker: if the issue is still open, re-implement or improve upon the closed work (e.g. a closed dependency-update PR may need a fresh PR for the next version).
Hard/complex tasks are never filtered for difficulty — they compete equally; upstream model-discovery in opencode.yml will map them to strong models when chosen.
Reply with ONLY the final implementation prompt for the chosen issue: a self-contained instruction for a coding agent that names the chosen issue number and how to implement it.
