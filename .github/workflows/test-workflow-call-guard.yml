# ponytail: test fixture for issue-201 — keep for this verification run, delete before final merge if guard is covered elsewhere
name: Test workflow-call guard (issue-201)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  call-opencode:
    uses: ./.github/workflows/opencode.yml
    with:
      prompt: "Reply with OK. Live test of the issue-201 trigger guard: this opencode.yml run is a workflow_call event, so the 'Trigger .github workflow tests' step must be skipped. issues/201"
    secrets: inherit
