# Issue 206 — Evidence pass 2026-09-26

Standing task: never close, never mark done. One evidence-driven analysis pass.

Read: AGENTS.md (agent-runner; no CLAUDE.md present).

## Analyzed window

Runs created after previous pass boundary `2026-09-25T19:11Z` through `2026-09-26T02:05Z` (current pass start).

## Verified runs (all inspected with `gh run view <id> --log-failed`)

### Hourly issue runner (`hourly-issue.yml`) — failures since cutoff

| Run | Created (UTC) | Conclusion | Failed job / step | Evidence |
|---|---|---|---|---|
| https://github.com/bacluc-agent/agent-runner/actions/runs/36205800162 | 2026-09-26T00:41:59Z | failure | run / run — `Run coordinator` (step 13) | `gh run view --log-failed` shows `fatal provider error (retry_limit)`; comment posted on #219 at 00:53:33Z |
| https://github.com/bacluc-agent/agent-runner/actions/runs/36203666410 | 2026-09-26T00:07:50Z | failure | run / run — `Run coordinator` (step 13) | Same `retry_limit` signature |
| https://github.com/bacluc-agent/agent-runner/actions/runs/36201573038 | 2026-09-25T23:34:53Z | failure | select — `Pick issue and dispatch` (step 6) | `fatal provider error` at selection stage (same provider-rate root cause) |
| https://github.com/bacluc-agent/agent-runner/actions/runs/36197579374 | 2026-09-25T22:37:10Z | failure | run / run — `Run coordinator` (step 13) | `retry_limit` |
| https://github.com/bacluc-agent/agent-runner/actions/runs/36195006602 | 2026-09-25T22:05:54Z | failure | run / run — `Run coordinator` (step 13) | `retry_limit` |
| https://github.com/bacluc-agent/agent-runner/actions/runs/36192597639 | 2026-09-25T21:38:03Z | failure | run / run — `Run coordinator` (step 13) | `retry_limit` |
| https://github.com/bacluc-agent/agent-runner/actions/runs/36189649754 | 2026-09-25T21:06:05Z | failure | run / run — `Run coordinator` (step 13) | `retry_limit` |
| https://github.com/bacluc-agent/agent-runner/actions/runs/36180642418 | 2026-09-25T19:35:44Z | failure | run / run — `Run coordinator` (step 13) | `retry_limit` |

### Review fixes runner (`review-fixes.yml`) — failures since cutoff

| Run | Created (UTC) | Conclusion | Failed job / step | Evidence |
|---|---|---|---|---|
| https://github.com/bacluc-agent/agent-runner/actions/runs/36206144669 | 2026-09-26T00:47:42Z | failure | `process-prs (bacluc-agent/renovate#1) / run` — `Run coordinator` (step 13) | `retry_limit` |

### OpenCode agent (`opencode.yml`) — failures since cutoff

No new failures on `main` in window (last `main` failure 36102537362 at 06:22:02Z, before cutoff).

### Refine issues (`refine-issues.yml`) — failures since cutoff

No new failures in window.

## Pattern analysis (only ≥2 distinct runs)

### Pattern A — Coordinator / selector fatal provider error (`retry_limit`) — 9 distinct runs

Runs: 36205800162, 36203666410, 36201573038, 36197579374, 36195006602, 36192597639, 36189649754, 36180642418 (hourly), 36206144669 (review fixes).

All fail at either `Run coordinator` (hourly/review-fixes `run` job) or `Pick issue and dispatch` (hourly `select` job) with the same `fatal provider error (retry_limit)` signature. The root cause chain is unchanged from previous passes: free OpenRouter models hit provider rate limits at runtime; `FAILED_TTL_HOURS = 2` in `model_availability.py` allows a rate-limited model to be re-selected after a trivial probe succeeds; no runtime coordinator fallback exists.

**Status: ALREADY COVERED.** This exact pattern is tracked by open improvement issue https://github.com/bacluc-agent/agent-todo/issues/247 ("Coordinator fatal provider error retry_limit; no runtime fallback", created 2026-09-21). The previous pass (run 36177527038, 2026-09-25) also documented it and did not file a duplicate. No new repo-side fix is warranted this pass — the design decision (runtime fallback vs. excluding free OpenRouter models from coordinator selection) remains open in #247.

### Pattern B — Selector `opencode=124` timeout — 0 new instances in window

No `select` job failures with `opencode=124` timeout in this window. Previous fix PR https://github.com/bacluc-agent/agent-runner/pull/65 (merged 2026-09-20) covers this. Not a recurrence.

### Pattern C — 120-min coordinator timeout with active work — 0 new instances in window

No new 120-min timeout runs in window. Previous pass noted this as a leftover; BacLuc rejected the watchdog fix (PR #16, closed). Not a new recurrence.

### Pattern D — Issue selection claim mechanism (`agent-running` label never applied) — covered by #279

No new evidence changes the finding from the previous pass (run 36177527038, 2026-09-25): `agent-running` is defined (`hourly-issue.yml:80`) and filtered (`hourly-issue.yml:90`, `refine-issues.yml:68`) but never applied (`grep -rn "add-label" .github/workflows/` returns only `agent-refined` at `refine-issues.yml:195`). `gh issue list -R bacluc-agent/agent-todo --state all --label agent-running` returns 0 of 300 issues. The claim mechanism issue https://github.com/bacluc-agent/agent-todo/issues/279 (filed 2026-09-25) covers this. No new repo-side fix proposed this pass — the fix design (anchored `SELECTED_ISSUE:` marker, non-fatal parse miss, `GITHUB_TOKEN` on release job) is recorded in #279 and awaits implementation.

### Pattern E — Review-fixes `process-prs` failures — 1 instance (36206144669)

Only one review-fixes failure in window; it is the same `retry_limit` coordinator failure (Pattern A), not a distinct review-fixes-specific pattern. Threshold (≥2) not met for a separate review-fixes pattern.

## Non-patterns (deliberately excluded)

- `36182415806` (hourly, `issue-279` branch, 2026-09-25 19:53Z) — PR-branch test run, not `main`; excluded from `main`-scoped analysis.
- `36154818145` / `36154818351` (`issue-277` branch) — PR-branch test runs; excluded.
- `36155165159` (`issue-277`, cancelled) — PR-branch; excluded.
- `36182415575` (OpenCode agent, `issue-279` branch) — PR-branch; excluded.

## Decision

- **No new improvement issue created.** Pattern A covered by #247; Pattern D covered by #279; Patterns B/C covered by merged PRs #65/#97; Pattern E is Pattern A in a different workflow.
- **No PR in `bacluc-agent/agent-runner` this pass.** The retry_limit fix requires a design decision (runtime fallback vs. model exclusion) that is open in #247; the claim-mechanism fix requires the anchored-marker design recorded in #279. Both are real design choices, not mechanical diffs, and are correctly left to the implementer.
- **No working branch with code changes.** Branch `issue-206-evidence-pass-2026-09-26` created for tracking; only this evidence file added.
- `bacluc-agent/agent-todo#206` remains **OPEN** (standing task — never close, never mark done).
- No closing keyword (`Fixes`, `Closes`, `Resolves`) used anywhere in this file or in the issue comment.

## Evidence links (absolute URLs, verified)

- Window boundary (previous pass): https://github.com/bacluc-agent/agent-runner/actions/runs/36177527038
- This pass run: https://github.com/bacluc-agent/agent-runner/actions/runs/36210591825 (in-progress at analysis time)
- Pattern A runs (all verified via `gh run view --log-failed`):
  - https://github.com/bacluc-agent/agent-runner/actions/runs/36205800162
  - https://github.com/bacluc-agent/agent-runner/actions/runs/36203666410
  - https://github.com/bacluc-agent/agent-runner/actions/runs/36201573038
  - https://github.com/bacluc-agent/agent-runner/actions/runs/36197579374
  - https://github.com/bacluc-agent/agent-runner/actions/runs/36195006602
  - https://github.com/bacluc-agent/agent-runner/actions/runs/36192597639
  - https://github.com/bacluc-agent/agent-runner/actions/runs/36189649754
  - https://github.com/bacluc-agent/agent-runner/actions/runs/36180642418
  - https://github.com/bacluc-agent/agent-runner/actions/runs/36206144669
- Existing improvement issues (not duplicated):
  - https://github.com/bacluc-agent/agent-todo/issues/247 (retry_limit)
  - https://github.com/bacluc-agent/agent-todo/issues/279 (claim mechanism)
- Merged fixes (not re-filed):
  - https://github.com/bacluc-agent/agent-runner/pull/65 (selector timeout)
  - https://github.com/bacluc-agent/agent-runner/pull/97 (timeout diagnostics)
  - https://github.com/bacluc-agent/agent-runner/pull/88 (previous evidence pass)

## Leftover questions for next pass

- Whether #247's proposed fix (runtime fallback or exclude free OpenRouter models) has been implemented.
- Whether #279's claim mechanism (anchored `SELECTED_ISSUE:` marker + non-fatal parse + `GITHUB_TOKEN` on release) has been implemented.
- Whether any new `main`-branch failures appear that are not covered by #247/#279.
