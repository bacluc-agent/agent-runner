[project]
name = "agent-todo"
version = "0.1.0"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "playwright==1.62.0",
    "pyotp==2.10.0",
    "pytest==9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["scripts", ".github/actions/model-availability"]
testpaths = ["scripts", ".github/actions/model-availability"]
